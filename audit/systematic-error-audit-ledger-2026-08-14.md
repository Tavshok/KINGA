# KINGA Systematic Error Audit Ledger

**Author:** Tavonga Shoko, Lead Engineer  
**Status:** Discovery in progress — no production behaviour changed  
**Audit principle:** Traceable evidence → deterministic interpretation → transparent uncertainty → defensible intelligence → controlled decision boundary.

## Audit Scope and Evidence Sources

| Audit domain | Evidence to trace | Initial authoritative contracts | Discovery status |
|---|---|---|---|
| Source-to-ledger integrity | Quote documents, extracted fields, submitted quote ledger, provenance, reconciliation | Canonical active-quote ledger and quote reconciliation contracts | Pending |
| L1/L2 intelligence | Evidence state, component coverage, tax/cost basis, comparison outputs, suppression state | Shared cost decision presentation and immutable R0 fixtures | Pending |
| Decision boundary | Evidence, finding, intelligence, recommendation, decision, settlement/savings suppression | Cost integrity and report decision contracts | Pending |
| Cross-report consistency | CL, CI, FR, Claims Manager, L2, Top Cost inputs and rendered decisions | Shared report presenters and same-snapshot fixtures | Pending |
| UI-to-backend truth | UI value/action, tRPC procedure, service, persistence, source evidence | Procedure contracts and client integrations | Pending |
| Negative and degraded states | Missing, malformed, duplicate, revised, incomplete, conflicting, unavailable evidence | Existing regression suites and no-write fixtures | Pending |

## Discovery Evidence Log

| Evidence ID | Area | Source examined | Result | Test/trace evidence | Status |
|---|---|---|---|---|---|
| AUD-BASE-001 | L2 submitted-price selection | `server/pipeline-v2/quoteOptimisationEngine.ts` | The composite builder selects lowest submitted active-quote price per component and retains benchmarks as comparison metadata only. | `buildCompositeQuote.test.ts`: 19 passing tests, including low/no benchmark, VAT/workshop fee, reconciliation, and missing-component cases. | Baseline verified |
| AUD-BASE-002 | Report cost decision boundary | `server/reporting/costIntegrity.ts`, `server/reporting/costDecisionPresentation.ts` | The report contract uses the active ledger, preserves incomplete-scope comparison intelligence, and withholds final L2/savings/settlement conclusions when scope or reconciliation is incomplete. | `costIntegrity.test.ts`, `costDecisionPresentation.test.ts`, and `r0FixtureModelAcceptance.test.ts`: 16 passing tests. | Baseline verified |
| AUD-OBS-001 | Incomplete composite benchmark fields | `stage-9-cost.ts` and `quoteOptimisationEngine.ts` | Incomplete composites persist benchmark comparison fields, but the inspected report decision contract does not consume them as settlement or savings conclusions. | Static producer-to-consumer trace; no confirmed user-visible divergence in this discovery pass. | Monitor in UI/API trace |

| AUD-P0-001 | Report quote detail authorization | `server/routers/quotes-core.ts` `quotes.getWithLineItems` | Every report-related quote query must derive and enforce authority from the claim object and caller tenant/role before returning quote rows. | The protected procedure called `getQuotesByClaimId(input.claimId)` without tenant or claim-object authorization. Its comment deliberately removed tenant filtering, so an authenticated caller who knew a foreign numeric claim ID could receive its quote rows and line items. | Missing object-level claim authorization at the procedure boundary. | P0 | Added target-claim lookup under a required tenant scope before quote/line-item reads. Ordinary callers are session-scoped; platform-super-admin requires explicit audited tenant selection. Restored the missing `inArray` query-helper import surfaced by the authorised-path regression. | `quotes-core.authorization.p0.test.ts`: same-tenant success, foreign numeric claim unavailable before read, explicit platform-super-admin selection. `tenant-isolation.test.ts`: 18 existing tenant tests. | 21 focused tests passed; bundled server and Vite production builds passed. | Corrected and re-verified |
| AUD-P1-001 | Legacy quote-ledger fallback | `server/reporting/costIntegrity.ts`, `client/src/components/KingaClaimsReport.tsx`, `server/db/assessment-db.ts` | Duplicate, superseded, and excluded quotation rows must not be represented as active submitted quotations or drive quote count/comparison presentation; when canonical ledger metadata is unavailable, the state must be explicitly qualified as legacy history rather than assumed active. | When `canonicalQuoteLedger` was absent, server `legacyQuoteRows` assigned every raw database quote `status: "active"`; the client top-cost fallback mapped every `quotes.getWithLineItems` row without ledger-status filtering. The underlying query returns all quote rows. A legacy/replayed duplicate could therefore appear as an active submitted quotation. | Raw-quote fallback had no canonical status/revision/deduplication boundary and no legacy-history qualification. | P1 | Added a shared legacy quote classifier. It recognises persisted rejected/cancelled state and parent-linked revised/adjusted/strip-requote supersession deterministically; all remaining raw rows are visible only as `legacy_unverified` history. They are excluded from active quote comparison, L1 derivation, savings, and settlement treatment. CL, CI, FR, actual client top-cost render, and client quote matrix disclose the qualification. | Executed CI/FR/CL renderer fixture, executed `KingaClaimsReport` top-cost render fixture, executed client quote-comparison state fixture, expanded cost integrity tests, and existing cost-decision/cross-surface strip tests. | 24 focused tests passed; bundled server and Vite production builds passed. | Corrected and re-verified |
| AUD-P1-002 | Stage 9 quote workflow status | `server/pipeline-v2/stage-9-cost.ts`, `server/pipeline-v2/canonicalQuoteLedger.ts` | Every persisted quotation must retain its workflow status and a distinct evidence-eligibility state. Only explicitly final-L2-eligible active submissions may enter L1/L2; withdrawn, commercial/process-rejected, integrity/scope-disqualified, and superseded prices remain traceable history with their reason. | The Stage 9 database fallback selected `panelBeaterQuotes.status` but did not carry it into `resolvedExtractedQuotes`. The canonical ledger therefore saw a rejected/cancelled repair quote as an ordinary repair submission and could classify it active unless another metadata rule excluded it. | Workflow status was lost between database fallback normalization and canonical ledger construction; the ledger had no evidence-eligibility boundary. | P1 | Propagated workflow status into normalized Stage 9 evidence. Added a separate eligibility model: submitted/active evidence is final-L2 eligible; withdrawn/cancelled and commercial/process-rejected evidence is comparison-only history; explicit integrity/scope disqualification is ineligible; revisions supersede their parent while retaining both records. The Stage 9 production final-L2 selector now admits only canonical active eligible repair evidence. | Ledger/selector execution and downstream history rendering passed. Real no-write `runCostOptimisationStage` mixed-status execution plus explicit all-noneligible final-conclusion suppression remains required. | Partial verification; production builds passed. | Open — real Stage 9 and final-conclusion suppression proof pending |
| AUD-P1-003 | Stage 9 decision-engine raw quote input | `server/pipeline-v2/stage-9-cost.ts`, `server/pipeline-v2/costDecisionEngine.ts` | Final cost-decision anomalies, highest-quote analysis, negotiation guidance, and recommendations must not treat comparison-only, ineligible, duplicate, or superseded prices as active decision inputs. Historical evidence may remain visible only with its eligibility reason. | Initial audit trace required confirmation because raw source evidence is retained in Stage 9. Direct inspection of the actual `runCostDecision` call shows `extracted_quotes: activeRepairQuotes.map(...)`, not `resolvedExtractedQuotes`. | Initial source traversal did not reach the final decision-call argument list before classification. | P1 | No correction required. The existing call uses the same active repair evidence population that supplies L1/L2 selection. | Direct call-site trace at `stage-9-cost.ts` lines 909–944. Future Stage 9 mixed-status execution remains part of AUD-P1-002 proof. | Not reproduced; no runtime behaviour changed. | Closed — false positive |
| AUD-P1-004 | Later Stage 9 composite and L1 raw quote paths | `server/pipeline-v2/stage-9-cost.ts` | Every later composite matrix, L1 baseline, evidence gate, and component-classification calculation must use the same eligible active repair evidence as initial L1/L2 selection. Comparison-only/ineligible, duplicate, and superseded evidence must remain separate history. | The early optimisation used `activeRepairQuotes`, but the later composite branch reassigned `allQuotes` from raw source evidence. It derived `repairQuotes`, `compositeInputQuotes`, `assessPipelineEvidenceGate(repairQuotes)`, and `allSubmittedTotalsForL1` from that raw population. Cancelled, rejected, explicitly ineligible, duplicate, or superseded prices could therefore re-enter composite L2 and L1. | Eligibility boundary was applied to the first optimisation path but not propagated to the later composite path. | P1 | Replaced later raw repair/composite/L1 populations with canonical `activeRepairQuotes`; the later evidence gate and matrix now share the same eligible set as initial L1/L2. Historical/ineligible evidence remains outside final L1/L2 calculation. | Direct Stage 9 later-path regression plus mixed-workflow canonical selection and formula tests. | Focused formula and Stage 9 tests passed; production builds passed. | Corrected and re-verified |
| AUD-P1-005 | L2 credibility gate is defined but unused | `server/pipeline-v2/quoteOptimisationEngine.ts` | L2 must apply one explicit, tested benchmark-validation rule and expose its selection path to evidence consumers. | `applyCredibilityGate` defined a 70% of P25 floor and 200% of P75 ceiling, but a project-wide invocation trace found no call site. The composite builder selected the lowest submitted active component price without applying a benchmark policy. | A partially implemented credibility rule remained disconnected from candidate selection and result qualification. | P1 | Retired the disconnected policy from the final L2 contract and implemented the user-confirmed rule directly in the composite builder: use `P50` where `abs(Qmin-P50)/P50 <= 30%`; otherwise use the lower of P50 and Qmin; retain submitted evidence; flag submitted-price spread above 20%. | Executable composite selection, tolerance-boundary, variance, L1/L2, Stage 9, report integrity, cross-surface, and client-contract tests. | 52 focused cross-surface tests plus production builds passed. | Corrected and re-verified |
| AUD-P1-006 | Report-surface L2 selection traceability | `server/reporting/*`, `client/src/components/ComponentCostMatrix.tsx` | CL, CI, FR, and client evidence surfaces must disclose benchmark selection method, benchmark deviation, and high line-item variance for each L2 component so the formula is auditable end to end. | The client `ComponentCostMatrix` maps and renders `l2SelectionMethod`, benchmark deviation, and line-item variance. A production field trace finds no equivalent server-report renderer mapping of these new component fields. | The benchmark-validated formula fields were added to composite/client contracts but not propagated to server report renderers. | P1 | Pending controlled correction: add the same component selection and variance trace to CL, CI, and FR through the shared report cost/evidence presenter; execute cross-surface render acceptance. | Pending | Direct repository field trace. | Open |
| AUD-L2-001 | Executable L2 formula baseline | `server/pipeline-v2/quoteOptimisationEngine.ts`, `buildCompositeQuote.test.ts` | Formula must be explicit, source-traceable, all-in, and decision-safe. | The prior composite L2 selected `min(submitted active component prices)` with benchmark P25/P50/P75 as non-selecting metadata; the disconnected 70%-of-P25 / 200%-of-P75 gate was not invoked. | Formula documentation, unused credibility code, and user-confirmed benchmark-validation policy diverged. | Policy change approved | Implemented the user-confirmed benchmark-validated formula: compare Qmin to P50; within 30% select P50; outside 30% select lower of P50 and Qmin; retain source evidence; add a scope-verification remark when like-for-like submitted-price spread exceeds 20%. | Composite selection, tolerance, variance, Stage 9 active-evidence, cost-integrity, report, and client contract tests. | 52 focused cross-surface tests and production builds passed after implementation. | Corrected and re-verified |
| AUD-P1-007 | Live CI quote-state presentation inconsistency | Real claim `12909902`; Claims Intelligence job `f269c560-94ba-4e37-8baa-df6eb52cf02c` | Visible submitted amounts, active/legacy status, L1 availability, highest quote, and quote comparison state must derive from the same canonical ledger population. | The report displayed two quotation amounts ($5,640 and $8,349), “2 quotes received,” and “Active market quote,” while also stating “0 active quotes,” legacy/non-active comparison evidence, and “Highest submitted quote $0.00.” | Live report path has divergent quote-state projections between visible quote list, high-level metrics, and cost-integrity boundary. | P1 | Pending controlled trace from reanalysed persisted quote rows through canonical ledger, CI data model, and renderer metrics. | Pending | User-authorised live reanalysis plus generated PDF text extraction. | Open |

## Finding Ledger

| ID | Area | Evidence | Expected | Actual | Root Cause | Severity | Correction | Regression | Verification | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| — | No confirmed finding yet | Discovery baseline only | — | — | — | — | — | — | — | Open |

## Classification Rules

Any divergence found during discovery is recorded before correction and classified as one of: **verified source value**, **documented revision**, **extraction defect**, **ledger reconstruction issue**, **scope difference**, **evidence gap**, **unresolved discrepancy**, or **pricing variance review signal**. A divergence is not silently normalised merely to make amounts reconcile.

## Correction Control

Only a confirmed defect with an identified affected component, intended invariant, severity, and bounded correction scope may move to implementation. Every correction requires deterministic regression evidence and a re-verification pass. Ambiguous intended behaviour remains a decision item rather than a speculative change.
