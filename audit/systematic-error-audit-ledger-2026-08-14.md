# KINGA Systematic Error Audit Ledger

**Author:** Tavonga Shoko, Lead Engineer  
**Status:** Controlled remediation in progress — production behaviour changes occur only in approved, checkpointed batches.  
**Audit principle:** Traceable evidence → deterministic interpretation → transparent uncertainty → defensible intelligence → controlled decision boundary.

## Audit Scope and Evidence Sources

| Audit domain | Evidence to trace | Initial authoritative contracts | Discovery status |
|---|---|---|---|
| Source-to-ledger integrity | Quote documents, extracted fields, submitted quote ledger, provenance, reconciliation | Canonical active-quote ledger and quote reconciliation contracts | Pending |
| L1/L2 intelligence | Evidence state, component coverage, tax/cost basis, comparison outputs, suppression state | Shared cost decision presentation and immutable R0 fixtures | Pending |
| Decision boundary | Evidence, finding, intelligence, recommendation, decision, settlement/savings suppression | Cost integrity and report decision contracts | Pending |
| Cross-report consistency | CL, CI, FR, Claims Manager, L2, Top Cost inputs and rendered decisions | Shared report presenters and same-snapshot fixtures | Pending |
| UI-to-backend truth | UI value/action, tRPC procedure, service, persistence, source evidence | Procedure contracts and client integrations | Pending |
| Negative and degraded states | Missing, malformed, duplicate, revised, incomplete, conflicting, unavailable evidence | Existing regression suites and no-write fixtures | Pending |

## Discovery Evidence Log

| Evidence ID | Area | Source examined | Result | Test/trace evidence | Status |
|---|---|---|---|---|---|
| AUD-BASE-001 | L2 submitted-price selection | `server/pipeline-v2/quoteOptimisationEngine.ts` | The composite builder selects lowest submitted active-quote price per component and retains benchmarks as comparison metadata only. | `buildCompositeQuote.test.ts`: 19 passing tests, including low/no benchmark, VAT/workshop fee, reconciliation, and missing-component cases. | Baseline verified |
| AUD-BASE-002 | Report cost decision boundary | `server/reporting/costIntegrity.ts`, `server/reporting/costDecisionPresentation.ts` | The report contract uses the active ledger, preserves incomplete-scope comparison intelligence, and withholds final L2/savings/settlement conclusions when scope or reconciliation is incomplete. | `costIntegrity.test.ts`, `costDecisionPresentation.test.ts`, and `r0FixtureModelAcceptance.test.ts`: 16 passing tests. | Baseline verified |
| AUD-OBS-001 | Incomplete composite benchmark fields | `stage-9-cost.ts` and `quoteOptimisationEngine.ts` | Incomplete composites persist benchmark comparison fields, but the inspected report decision contract does not consume them as settlement or savings conclusions. | Static producer-to-consumer trace; no confirmed user-visible divergence in this discovery pass. | Monitor in UI/API trace |

| AUD-P0-001 | Report quote detail authorization | `server/routers/quotes-core.ts` `quotes.getWithLineItems` | Every report-related quote query must derive and enforce authority from the claim object and caller tenant/role before returning quote rows. | The protected procedure called `getQuotesByClaimId(input.claimId)` without tenant or claim-object authorization. Its comment deliberately removed tenant filtering, so an authenticated caller who knew a foreign numeric claim ID could receive its quote rows and line items. | Missing object-level claim authorization at the procedure boundary. | P0 | Added target-claim lookup under a required tenant scope before quote/line-item reads. Ordinary callers are session-scoped; platform-super-admin requires explicit audited tenant selection. Restored the missing `inArray` query-helper import surfaced by the authorised-path regression. | `quotes-core.authorization.p0.test.ts`: same-tenant success, foreign numeric claim unavailable before read, explicit platform-super-admin selection. `tenant-isolation.test.ts`: 18 existing tenant tests. | 21 focused tests passed; bundled server and Vite production builds passed. | Corrected and re-verified |
| AUD-P1-001 | Legacy quote-ledger fallback | `server/reporting/costIntegrity.ts`, `client/src/components/KingaClaimsReport.tsx`, `server/db/assessment-db.ts` | Duplicate, superseded, and excluded quotation rows must not be represented as active submitted quotations or drive quote count/comparison presentation; when canonical ledger metadata is unavailable, the state must be explicitly qualified as legacy history rather than assumed active. | When `canonicalQuoteLedger` was absent, server `legacyQuoteRows` assigned every raw database quote `status: "active"`; the client top-cost fallback mapped every `quotes.getWithLineItems` row without ledger-status filtering. The underlying query returns all quote rows. A legacy/replayed duplicate could therefore appear as an active submitted quotation. | Raw-quote fallback had no canonical status/revision/deduplication boundary and no legacy-history qualification. | P1 | Added a shared legacy quote classifier. It recognises persisted rejected/cancelled state and parent-linked revised/adjusted/strip-requote supersession deterministically; all remaining raw rows are visible only as `legacy_unverified` history. They are excluded from active quote comparison, L1 derivation, savings, and settlement treatment. CL, CI, FR, actual client top-cost render, and client quote matrix disclose the qualification. | Executed CI/FR/CL renderer fixture, executed `KingaClaimsReport` top-cost render fixture, executed client quote-comparison state fixture, expanded cost integrity tests, and existing cost-decision/cross-surface strip tests. | 24 focused tests passed; bundled server and Vite production builds passed. | Corrected and re-verified |
| AUD-P1-002 | Stage 9 quote workflow status | `server/pipeline-v2/stage-9-cost.ts`, `server/pipeline-v2/canonicalQuoteLedger.ts` | Every persisted quotation must retain its workflow status and a distinct evidence-eligibility state. Only explicitly final-L2-eligible active submissions may enter L1/L2; withdrawn, commercial/process-rejected, integrity/scope-disqualified, and superseded prices remain traceable history with their reason. | The Stage 9 database fallback selected `panelBeaterQuotes.status` but did not carry it into `resolvedExtractedQuotes`. The canonical ledger therefore saw a rejected/cancelled repair quote as an ordinary repair submission and could classify it active unless another metadata rule excluded it. | Workflow status was lost between database fallback normalization and canonical ledger construction; the ledger had no evidence-eligibility boundary. | P1 | Propagated workflow status into normalized Stage 9 evidence. Added a separate eligibility model: submitted/active evidence is final-L2 eligible; withdrawn/cancelled and commercial/process-rejected evidence is comparison-only history; explicit integrity/scope disqualification is ineligible; revisions supersede their parent while retaining both records. The Stage 9 production final-L2 selector now admits only canonical active eligible repair evidence. | Ledger/selector execution and downstream history rendering passed. Real no-write `runCostOptimisationStage` mixed-status execution plus explicit all-noneligible final-conclusion suppression remains required. | Partial verification; production builds passed. | Open — real Stage 9 and final-conclusion suppression proof pending |
| AUD-P1-003 | Stage 9 decision-engine raw quote input | `server/pipeline-v2/stage-9-cost.ts`, `server/pipeline-v2/costDecisionEngine.ts` | Final cost-decision anomalies, highest-quote analysis, negotiation guidance, and recommendations must not treat comparison-only, ineligible, duplicate, or superseded prices as active decision inputs. Historical evidence may remain visible only with its eligibility reason. | Initial audit trace required confirmation because raw source evidence is retained in Stage 9. Direct inspection of the actual `runCostDecision` call shows `extracted_quotes: activeRepairQuotes.map(...)`, not `resolvedExtractedQuotes`. | Initial source traversal did not reach the final decision-call argument list before classification. | P1 | No correction required. The existing call uses the same active repair evidence population that supplies L1/L2 selection. | Direct call-site trace at `stage-9-cost.ts` lines 909–944. Future Stage 9 mixed-status execution remains part of AUD-P1-002 proof. | Not reproduced; no runtime behaviour changed. | Closed — false positive |
| AUD-P1-004 | Later Stage 9 composite and L1 raw quote paths | `server/pipeline-v2/stage-9-cost.ts` | Every later composite matrix, L1 baseline, evidence gate, and component-classification calculation must use the same eligible active repair evidence as initial L1/L2 selection. Comparison-only/ineligible, duplicate, and superseded evidence must remain separate history. | The early optimisation used `activeRepairQuotes`, but the later composite branch reassigned `allQuotes` from raw source evidence. It derived `repairQuotes`, `compositeInputQuotes`, `assessPipelineEvidenceGate(repairQuotes)`, and `allSubmittedTotalsForL1` from that raw population. Cancelled, rejected, explicitly ineligible, duplicate, or superseded prices could therefore re-enter composite L2 and L1. | Eligibility boundary was applied to the first optimisation path but not propagated to the later composite path. | P1 | Replaced later raw repair/composite/L1 populations with canonical `activeRepairQuotes`; the later evidence gate and matrix now share the same eligible set as initial L1/L2. Historical/ineligible evidence remains outside final L1/L2 calculation. | Direct Stage 9 later-path regression plus mixed-workflow canonical selection and formula tests. | Focused formula and Stage 9 tests passed; production builds passed. | Corrected and re-verified |
| AUD-P1-005 | L2 credibility gate is defined but unused | `server/pipeline-v2/quoteOptimisationEngine.ts` | L2 must apply one explicit, tested benchmark-validation rule and expose its selection path to evidence consumers. | `applyCredibilityGate` defined a 70% of P25 floor and 200% of P75 ceiling, but a project-wide invocation trace found no call site. The composite builder selected the lowest submitted active component price without applying a benchmark policy. | A partially implemented credibility rule remained disconnected from candidate selection and result qualification. | P1 | Retired the disconnected policy from the final L2 contract and implemented the user-confirmed rule directly in the composite builder: use `P50` where `abs(Qmin-P50)/P50 <= 30%`; otherwise use the lower of P50 and Qmin; retain submitted evidence; flag submitted-price spread above 20%. | Executable composite selection, tolerance-boundary, variance, L1/L2, Stage 9, report integrity, cross-surface, and client-contract tests. | 52 focused cross-surface tests plus production builds passed. | Corrected and re-verified |
| AUD-P1-006 | Report-surface L2 selection traceability | `server/reporting/costIntegrity.ts`, `server/reporting/costDecisionPresentation.ts`, CL/CI/FR generators, `client/src/components/ComponentCostMatrix.tsx` | CL, CI, FR, and client evidence surfaces must disclose benchmark selection method, benchmark deviation, and high line-item variance for each L2 component so the formula is auditable end to end. | The client `ComponentCostMatrix` maps and renders `l2SelectionMethod`, benchmark deviation, and line-item variance. | The shared server-side mapper and presentation panel existed but had not been proven through actual CL, CI, and FR generator execution with persisted-shaped benchmark evidence. | P1 | Added executed cross-report generator acceptance using benchmark-selected, outside-30%-lower-value, and >20%-spread component rows. CL, CI, and FR each render the shared L2 Component Validation panel while retaining the unchanged L2 decision amount. | Executed generator fixture, direct presentation regression, and mixed-status no-write Stage 9 evidence test. | 37 focused tests and bundled server/Vite builds passed. | Corrected and re-verified |
| AUD-L2-001 | Executable L2 formula baseline | `server/pipeline-v2/quoteOptimisationEngine.ts`, `buildCompositeQuote.test.ts` | Formula must be explicit, source-traceable, all-in, and decision-safe. | The prior composite L2 selected `min(submitted active component prices)` with benchmark P25/P50/P75 as non-selecting metadata; the disconnected 70%-of-P25 / 200%-of-P75 gate was not invoked. | Formula documentation, unused credibility code, and user-confirmed benchmark-validation policy diverged. | Policy change approved | Implemented the user-confirmed benchmark-validated formula: compare Qmin to P50; within 30% select P50; outside 30% select lower of P50 and Qmin; retain source evidence; add a scope-verification remark when like-for-like submitted-price spread exceeds 20%. | Composite selection, tolerance, variance, Stage 9 active-evidence, cost-integrity, report, and client contract tests. | 52 focused cross-surface tests and production builds passed after implementation. | Corrected and re-verified |
| AUD-P1-007 | Live CI quote-state presentation inconsistency | Real claim `12909902`; Claims Intelligence job `f269c560-94ba-4e37-8baa-df6eb52cf02c`; `claimsIntelligenceReport.ts`; `costIntegrity.ts` | Visible submitted amounts, active/legacy status, L1 availability, highest quote, and quote comparison state must derive from the same canonical ledger population. | The report displayed two quotation amounts ($5,640 and $8,349), “2 quotes received,” and “Active market quote,” while also stating “0 active quotes,” legacy/non-active comparison evidence, and “Highest submitted quote $0.00.” | CI displayed `submittedQuotes` but derived highest quote, savings, L1 support, and active line-item state from `activeQuotes`. With absent ledger metadata, visible raw rows were qualified history while metrics were correctly empty; the renderer then mislabelled that history active and printed a false numeric zero. | P1 | Added one typed `resolveReportQuoteEvidencePresentation` contract. CI cards, visible record count, active comparison count, highest active quote, and legacy labels now consume that contract. Legacy history remains visible but is explicitly not active comparison evidence; absent active metrics render unavailable, never `$0.00`. | Executed shared projection, no-write CI renderer, and read-only live CI HTML checks. | 36 focused tests and bundled server/Vite builds passed. The selected live claim was not rerun and no report job or business record was created. | Corrected and re-verified |
| AUD-P1-008 | Stage 9 benchmark helper split failure suppresses canonical ledger persistence | Real claim `12909902`, assessment `20370001`; `server/db/intelligence-db.ts`; `server/pipeline-v2/stage-9-cost.ts` | Canonical quote-ledger state must persist whenever Stage 9 receives submitted repair evidence; a benchmark-source failure may qualify L2 but must not erase the ledger, quote state, or report trace. | The persisted assessment contains two selected quotes, 49 damaged components, 29/27 persisted quote lines, and ordinary quote optimisation, but `perComponentBenchmarks` and `compositeOptimisation` are null. The canonical ledger is therefore absent from report input. | The split `intelligence-db.ts` module referenced `schema.componentRepairOutcomes` without importing/defining `schema`, throwing `ReferenceError: schema is not defined`. Stage 9 caught the failure before it attached the ledger. Its start-hook mapping also omitted Stage 9, so no `pipeline_jobs` row was opened for the completed real run. | P1 | Restored the schema namespace import. Stage 9 now persists a canonical quote-state baseline immediately after normal cost output construction and before optional benchmark enrichment. Added the missing `Stage 9 — Cost Optimisation` start mapping and start hook, preserving a canonical `9_cost` job lifecycle for future runs. | Executed actual 49-component benchmark helper recheck, no-write real Stage 9 execution, Stage 9 persistence/observability regressions, and focused report regression matrix. | 36 focused tests and bundled server/Vite builds passed. No real claim rerun, claim write, quote write, payment, settlement, policy, or report job was performed by the correction. | Corrected and re-verified |
| AUD-L2-002 | Ordinary quality findings and evidence gaps caused L2 to disappear | `quoteOptimisationEngine.ts`; `stage-9-cost.ts`; `shared/costDecisionPresentation.ts`; CL/CI/FR and client cost surfaces | KINGA Optimised Quote must remain visible in every evidence state. Where eligible submitted component scope is incomplete, the system must show the supported priced scope or state that no supported component amount exists, explain the limitation, and recommend human review without inventing cost. A defective, total-only, historical, or ineligible quote must not suppress L2 from other eligible submitted component evidence. | Previous contracts rendered non-complete L2 as pending, evidence-qualified, or not published. Header/reconciliation and ordinary quality warnings also previously suppressed otherwise supported output. | Cost-computation completeness, cost-decision presentation, and report wording treated a lack of final all-in authority as a reason to hide the KINGA Optimised Quote section instead of presenting its supported basis and review requirement. | Product policy correction | Added `human_review_required` cost-decision state. Complete scopes retain numeric L2; partial submitted component coverage presents the priced scope explicitly as not all-in; no supported component price presents a visible human-review state with any eligible whole-quote anchor separately labelled as not component-optimised. Savings and settlement authority remain outside review-required states. Inferred/proportional line prices remain excluded; remaining eligible submitted evidence continues to drive L2. | Direct composite, real no-write Stage 9, shared presentation, client rendering, and executed CL/CI/FR rendering fixtures covering complete, partial, no-supported-component, defective/inferred, total-only, historical, ineligible, superseded, and revised evidence. | 96 focused tests and bundled server/Vite production builds passed. No live claim, quote, assessment, policy, payment, settlement, or report job was changed by this batch. | Corrected and re-verified |

| AUD-P1-009 | Inconsistent repairability thresholds and isolated structural treatment | `pipelineCostConstants.ts`; Stage 5, Stage 7, final assessment persistence, truth reconciliation, shared repairability presentation | KINGA must apply the user-confirmed 70% economic write-off recommendation threshold consistently, using complete L2 and verified market value. Technical write-off requires combined KINGA dedicated structural-damage and executed severe/catastrophic physics evidence. Neither path can create settlement authority. | Economic thresholds were split across 65%, 75%, and a separate cost-tier classification. Reports only consumed a persisted total-loss flag, while structural evidence could lead only to a generic review state. | Decision and presentation paths had no shared recommendation reducer; preliminary cost/physics contexts and final report outputs diverged. | P1 | Added one shared KINGA write-off reducer. Economic recommendation uses complete L2 plus verified market value at ≥70%; technical recommendation requires structural detection plus executed severe/catastrophic physics; combined evidence is explicit. Final persistence carries the recommendation in cost intelligence and the report/client repairability card consumes the same record. | Direct 69.99%/70% boundary, technical-only, combined, missing-market review, all-ineligible human-review, client, and CL/CI/FR rendered acceptance. | 92 focused tests and bundled production builds passed. No live claim, quote, assessment, policy, payment, settlement, or report job was changed. | Corrected and re-verified |

| AUD-P1-010 | Claim Truth cost resolver reintroduced raw quotation authority | `claimTruthLayer.ts`; `orchestrator.ts`; Stage 9 canonical ledger | Cost verdicts, quote deviations, negotiation guidance, fraud indicators, recommendations, savings, and settlement-facing conclusions must consume the canonical eligible repair-quote population only. Cancelled, rejected, superseded, duplicate, comparison-only, and explicitly ineligible evidence remains transparent history but has no decision authority. | The live orchestrator passed raw Stage 3 `extracted_quotes` into Claim Truth. Claim Truth then added a Stage 5 assembled repair quote and calculated lowest quote, deviations, negotiation savings, and downstream cost signals from those raw values. | The earlier Stage 9 active-evidence correction had not been propagated into the separately persisted Claim Truth path. The dormant Decision Optimisation Engine was not the live path; Claim Truth was the production decision source. | P1 | The orchestrator now derives Claim Truth candidates from `compositeOptimisation.canonicalQuoteLedger` entries with `active` status and `final_l2_eligible` evidence. Where Stage 9 has no eligible entries, Claim Truth receives an explicit empty authority population and produces insufficient-data/review state rather than falling back to raw quote prices. | Direct Claim Truth canonical-evidence and live hand-off acceptance; full Stage 9 mixed-status assertion of high quote, anomalies, recommendation, guidance, and excluded names; eligible-only versus raw-history Claim Truth decision/fraud equivalence; cross-report/client/L2 and write-off matrix. | 16 focused files / 96 tests passed; bundled server and Vite production builds passed (existing client chunk-size warnings only). No live claim, quote, assessment, policy, payment, settlement, recovery, authority, or report-job record changed. | Corrected and re-verified |
| AUD-P1-012 | Potential savings absent from concise KINGA Optimised Quote presentation | `shared/costDecisionPresentation.ts`; report cost-decision renderer; `KingaClaimsReport.tsx` | Where complete eligible L1 and L2 exist and L1 exceeds L2, KINGA should show Potential savings without exposing calculation mechanics to the client or creating settlement authority. | Savings was calculated in Stage 9 but absent from concise summary surfaces. Client review states also exposed partial-scope calculation labels and a whole-quote anchor. | The shared cost-decision contract carried no savings field, and client rendering reused professional evidence-qualified detail. | P1 | Added an evidence-bound savings field to the shared contract. It is shown only for complete eligible L1/L2 evidence and positive savings. Client top-cost shows concise KINGA Optimised Quote and Potential savings, while human-review states reveal no partial calculation or anchor. Report summaries show the same concise savings label; professional verification detail remains separate. | Shared contract, client rendering, CL/CI/FR cost-decision rendering, review-state suppression, R0 snapshot, canonical-ledger, Claim Truth, write-off regression matrix, and ComponentCostMatrix benchmark/variance client acceptance. | 14 focused files / 89 tests passed; bundled server and Vite production builds passed. No live claim, quote, assessment, policy, payment, settlement, recovery, authority, or report-job record changed. | Corrected and re-verified |

| AUD-P1-014 | Claimant benchmark-mechanics disclosure boundary | `client/src/lib/reportAudience.ts`; `ClaimDecisionReport.tsx`; `InsurerComparisonView.tsx`; `KingaClaimsReport.tsx`; `ComponentCostMatrix.tsx` | A claimant must receive concise KINGA Optimised Quote and Potential savings outcomes only. Benchmark source, model, sample, stratum, fallback, and selection mechanics are internal professional evidence. | Component-level client suppression and role classification existed, but there was no execution proof that the actual ClaimDecisionReport standard composition retained that boundary. | Route-level composition was not isolated for deterministic rendering acceptance, leaving an unverified possibility that a future prop omission could reintroduce professional evidence into claimant output. | P1 | Added `ClaimDecisionReportStandardView` as the actual standard route composition and kept ClaimDecisionReport bound to `getKingaClaimsReportAudience(user?.role)`; InsurerComparisonView remains explicitly professional. | Rendered canonical active-quote/Stage 9 benchmark evidence under claimant and professional audiences. Claimant output contains KINGA Optimised Quote and Potential savings but excludes `Market Benchmark`, `Stat n=8`, internal comparison, fallback, sample, and selection labels; professional output contains those internal labels. | 3 disclosure tests plus the 13-test disclosure/L2 regression group passed. Bundled server and Vite production builds passed. No live claim, quote, assessment, policy, payment, settlement, recovery, authority, or report-job record changed. | Corrected and re-verified |
| AUD-P1-015 | Apparent direct `Severity Benchmark` claimant label | `client/src/pages/ClaimDecisionReport.tsx`; actual standard ClaimDecisionReport composition | No client report path may display a benchmark label. | Static inspection found an obsolete `severity_fallback: "Severity Benchmark"` mapping in the legacy `CostDecision` helper. | The helper is declared but has no invocation in ClaimDecisionReport; it is not part of the actual standard or forensic report composition. | P1 | No production behaviour correction was needed. Added actual claimant and professional standard-report rendering assertions that both exclude the obsolete label. | Source call-site trace plus actual standard-report rendering with Stage 9 benchmark evidence. | Disclosure acceptance passed; production builds passed. No live record changed. | Not reproduced — closed false positive |
| AUD-L2-TRACE-002 | Material source-to-decision L1/L2 trace | Submitted source quotes; Stage 9 canonical ledger/composite; shared presentation; CL/CI/FR/client rendering | Every material displayed L1/L2 value must be traceable to eligible source evidence and the persisted Stage 9 composite; no renderer may recalculate or invent a substitute. | One persisted-shaped no-write fixture carried USD 1,000 and USD 1,200 source quotes, L1 USD 1,000, P50 USD 900, L2 USD 900, and savings USD 100. | No production divergence was reproduced. An initial test fixture selected the documented hybrid statistical model before vehicle-specific fallback; this was a fixture-scope distinction, not a ledger or renderer defect. | P1 | Added actual Stage 9-to-shared-contract-to-CL/CI/FR/client acceptance. | `l1L2SourceDecisionTrace.p1.test.ts` plus existing Stage 9 canonical and cross-surface trace contracts. | 19 focused tests and production builds passed; no live record changed. | Verified — no defect reproduced |
| AUD-P1-016 | Tenant-rate override loader reference failure | `server/db.ts` assessment entry path; `server/db/intelligence-db.ts` export | Optional configured tenant rate overrides should load when present; a non-fatal fallback must not be caused by an unresolved local function reference. | Development runtime reported `ReferenceError: getTenantRates is not defined`; static inspection found the call in `server/db.ts` and the exported function in `server/db/intelligence-db.ts`, with no corresponding import. | The error was caught and regional defaults allowed the assessment to continue, but configured tenant-specific overrides were skipped on this path. | P1 | Approved minimal correction added the missing `getTenantRates` import only. No rate, formula, persistence, or workflow change. | Isolated configured-override/null-fallback regression plus static assessment-entry reference assertion. | 14 focused tests and production builds passed; no claim rerun or record changed. | Corrected and verified |
| AUD-P1-017 | Claims Manager raw lowest-quote authority divergence | `client/src/pages/ClaimsManagerComparisonView.tsx`; `client/src/lib/claimsManagerCostProjection.ts` | Decision-facing comparison must use canonical eligible quote ledger/L1/L2 authority and retain non-eligible quote history separately. | The view previously fetched `quotes.byClaim` and reduced raw `quotedAmount` directly. | Non-eligible history could become “Lowest Quote,” diverging from CL/CI/FR/client decision authority. | P1 | Approved correction added one canonical persisted Stage 9 projection; raw rows outside the active/final-L2-eligible ledger set are retained as Quote history and excluded from L1/L2. | Projection unit acceptance and actual rendered Claims Manager fixture. | 13 focused tests and production builds passed; no stored record changed. | Corrected and verified |
| AUD-P1-018 | Claims Manager lowest-quote raw-cent display | `client/src/pages/ClaimsManagerComparisonView.tsx` | Monetary values stored in cents must be scaled once before professional display. | Actual isolated rendering previously passed `quotedAmount: 80000` and displayed `$80,000` under “Lowest Quote.” | The summary overstated the quoted value by 100×. | P1 | All Claims Manager quote and summary totals now pass cents to the tenant formatter exactly once; the corrected render shows USD 800.00. | Actual SSR acceptance in `crossReportUiTruthAudit.p1.test.ts`. | 13 focused tests and production builds passed; no stored record changed. | Corrected and verified |
| AUD-P1-019 | Claims Manager KINGA assessment cent double-scale | `client/src/pages/ClaimsManagerComparisonView.tsx` | `estimatedCost` is stored in cents and must be passed to the tenant formatter once. | Actual rendering passed `estimatedCost: 150000`; the view uses `fmt(aiCost * 100)` and rendered USD 150,000.00. | The professional KINGA assessment cost is overstated by 100×. | P1 | No correction in audit-only batch. Requires separately approved presentation-only unit correction. | Actual SSR unit fixture in `crossReportUiTruthAudit.p1.test.ts`. | 6 focused tests and production builds passed; finding reproduced. | Open |
| AUD-P1-020 | Claims Manager KINGA-versus-assessor mixed-unit variance | `client/src/pages/ClaimsManagerComparisonView.tsx` | Variance operands must share the same unit. | `assessorCost` is converted from cents to dollars while `aiCost` remains cents before `calculateVariance`; the USD 1,000 / USD 1,500 fixture rendered -99.3%. | The professional variance is materially wrong; correct result is -33.3%. | P1 | No correction in audit-only batch. Correct with AUD-P1-019 by using cents consistently or converting both operands once. | Actual SSR unit fixture in `crossReportUiTruthAudit.p1.test.ts`. | 6 focused tests and production builds passed; finding reproduced. | Open |

## Finding Ledger

| ID | Area | Evidence | Expected | Actual | Root Cause | Severity | Correction | Regression | Verification | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| — | No confirmed finding yet | Discovery baseline only | — | — | — | — | — | — | — | Open |

## Classification Rules

Any divergence found during discovery is recorded before correction and classified as one of: **verified source value**, **documented revision**, **extraction defect**, **ledger reconstruction issue**, **scope difference**, **evidence gap**, **unresolved discrepancy**, or **pricing variance review signal**. A divergence is not silently normalised merely to make amounts reconcile.

## Correction Control

Only a confirmed defect with an identified affected component, intended invariant, severity, and bounded correction scope may move to implementation. Every correction requires deterministic regression evidence and a re-verification pass. Ambiguous intended behaviour remains a decision item rather than a speculative change.
