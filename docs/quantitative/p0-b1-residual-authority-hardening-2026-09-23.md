# P0-B1 Residual Authority Hardening

- **Date:** 23 September 2026
- **Status:** Merge readiness complete; ready for owner-review PR
- **Scope:** A narrow, independent hardening package. It is not P0-B1 integration or P0-B1-Client.

## Purpose

The final review of the rebased P0-B1 integration identified three reachable server-side fraud-authority bypasses. This package applies the existing P0-B1 rule to those paths without expanding into the paused integration or browser remediation work.

> **P0-B1 rule:** Until a future owner-approved qualified fraud authority exists, stored fraud score, level, indicator, trend, threshold classification, escalation conclusion, routing conclusion, certification, and fraud-derived publication cannot govern or appear in an automated output. An affected surface must preserve independently supported non-fraud content where it is safe to do so and emit the shared actionable P0-B1 hold.

The canonical structured hold is `buildP0B1FraudDecisionHold()`; protected tRPC procedures use `throwP0B1FraudDecisionHold()`. The latter includes the required-evidence and resolution guidance from the immutable shared contract.

## Implemented boundary

| Surface                                                    | Prior bypass                                                                                                                                                         | Remediation                                                                                                                                                                                                                                      | Preserved boundary                                                                                                                                        |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Legacy Forensic Analysis Report                            | `generateForensicReport()` rendered stored `fraud_score`, `fraud_risk_level`, and recommendation as a scorecard.                                                     | The scorecard is replaced by `renderP0B1FraudAbstentionMarker()`. The exported renderer has a direct behavioral regression against the rich tenant-owned fixture.                                                                                | Claim identity, vehicle, dates, permitted input-fidelity information, cost content, and the independent P0-A-2 collision-physics marker remain available. |
| `exceptionIntelligence.getSystemDriftReport`               | The route read historic `fraudScore` values, calculated period averages, applied severity thresholds, and rendered a `Fraud Score Distribution` conclusion.          | After user and tenant validation, the route throws the canonical P0-B1 hold before `getDb()` or any aggregate read. The historic score select, average, threshold, and narrative are removed.                                                    | Authentication and tenant validation remain first. No held request opens a database handle.                                                               |
| Executive fraud trend, distribution, and escalation routes | `getFraudDetectionTrends`, `getFraudRiskDistribution`, and `getEscalationQueue` queried or projected fraud classifications through executive data paths.             | Each remains bound directly to `executiveProcedure`, then throws the canonical P0-B1 hold before a data read.                                                                                                                                    | `executiveProcedure` preserves insurer-domain tenant context and executive-role denial before the hold.                                                   |
| Executive operational detail middleware risk               | The earlier route-level ordering repair could be silently defeated if a shared fraud middleware wrapped the procedure and held before route-level tenant validation. | The package explicitly asserts that the operational-detail route remains directly bound to `executiveProcedure`; its existing session-derived scope resolution, tenant validation, cross-tenant audit, and only then canonical hold stay intact. | A wrong-role, tenantless, or unauthorized cross-tenant request retains its pre-hold behavior and does not reveal a hold or object-existence signal.       |

## Focused validation

All validation uses the guarded disposable `kinga_ci_test` runner only.

| Command                                                                                                                                                            | Result                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Initial three-suite behavioral checkpoint                                                                                                                          | **3 files / 22 tests passed**                                                                                                                                                                                                                                                                                                                                                                |
| Post-correction boundary matrix — executive, exception-intelligence, legacy forensic report, fraud presentation, and the two merged independent hardening packages | **7 files / 40 tests passed** after the tenantless-super-admin correction.                                                                                                                                                                                                                                                                                                                   |
| Static source audit                                                                                                                                                | The direct Legacy Forensic Analysis renderer contains no fraud score, level, indicator, finding, or recommendation field projection; drift holds before `getDb()`; the three executive fraud endpoints remain direct `executiveProcedure` bindings and first require a non-null tenant scope; no `p0FraudExecutiveProcedure` wrapper remains.                                                |
| Type compatibility                                                                                                                                                 | The held drift procedure retains its historical response type annotation solely to avoid changing the typed browser contract; the executable branch always throws before constructing or publishing that payload. Direct `tsc --noEmit` exits 2 on inherited project diagnostics, with no diagnostic on the active changed executive, drift, forensic renderer, or regression-test paths.    |
| `git diff --check`                                                                                                                                                 | Passed at focused-validation checkpoint.                                                                                                                                                                                                                                                                                                                                                     |
| Fresh independent adversarial review                                                                                                                               | **APPROVE.** The reviewer separately verified the shared insurer middleware’s tenantless platform-super-admin behavior; executive role → tenant scope → canonical hold ordering for all three fraud routes; no post-hold DB reads; the unwrapped operational-detail authorization/audit sequence; drift no-DB behavior; direct forensic sentinels; canonical shared holds; and narrow scope. |
| Guarded full suite                                                                                                                                                 | **4 deterministic serial shards passed:** **616 files** — 615 passed and 1 skipped; **9,730 tests passed** and 4 skipped. The standard suite configuration excludes two real-LLM integration tests and the `server/scripts/**` debug-script path. An earlier monolithic run stopped without a Vitest summary, so it was not accepted as evidence.                                            |
| Production build                                                                                                                                                   | **Passed:** Vite client build and bundled production server entrypoint completed. The Vite large-chunk notice is an existing bundle-size warning, not a build failure.                                                                                                                                                                                                                       |

The executive regression covers each held fraud endpoint for a valid executive session and an unauthorized claims-processor session. The drift regression proves a tenant-scoped caller reaches the canonical hold without invoking `getDb()`, while a tenantless caller is denied before the hold. The legacy forensic regression uses the tenant-owned stored-score fixture and proves its scorecard is replaced by the actionable marker.

### Adversarial review correction

The first independent review correctly blocked the executive endpoints for a shared-middleware interaction: `insurerDomainProcedure` permits a platform super-admin without a session tenant to reach an insurer procedure so routes with explicit cross-tenant selection can resolve that selection. The three fraud-only executive routes accept no tenant-selection input, so they could have returned a P0 hold to that unscoped admin before tenant authorization. `requireExecutiveFraudTenantScope()` now runs after the executive role guard and before the canonical hold for all three routes. It rejects the unscoped super-admin without a database call. `getOperationalClaimDetail` remains separate because it does support and validates explicit cross-tenant selection before its hold. The forensic regression now also proves the stored indicator, indicator finding, and recommendation label do not reach the direct renderer.

## Deliberate exclusions

This package does not modify schema, migrations, seed data, staging, production, credentials, deployment, P0-B1 integration, P0-B1-Client Package A, or deferred P0-B1-Client Packages B and C. Audit-export policy remains a separately tracked near-term policy item; it is not treated as a qualified fraud authority here.

## Merge-readiness requirements

The required fresh adversarial review returned **APPROVE** after the tenantless-super-admin correction. The complete guarded full suite, production build, changed-path type-diagnostic confirmation, formatting and whitespace hygiene, and this evidence record now satisfy merge readiness. This narrow package may be committed, pushed, and opened as an owner-review PR, but it must not merge without owner approval. After the package merges, P0-B1 integration resumes from its preserved branch and must perform the owner-required shared-middleware/wrapper risk audit before its PR is opened.
