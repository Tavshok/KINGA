export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";
// Key used by the active provider navigation boundary to store the post-login
// return path before redirecting. Login.tsx consumes it after the callback.
export const RETURN_PATH_STORAGE_KEY = "kinga_post_login_return_path";
